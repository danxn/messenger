# messenger

`git checkout chat`

## Install

`yarn install`

## Start

`yarn start`
`cd api`
`yarn install`

Create file .env in api folder and write to it:
`PORT=3001`

Then

`DEBUG=api:* yarn start`
